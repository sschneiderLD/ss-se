# LaunchDarkly Client-side ID (find in Project Settings > Environments)
# Use the Test environment's client-side ID for development
VITE_LAUNCHDARKLY_CLIENT_ID=your-client-side-id-here

# Optional: Backend API URL for production (leave unset to use Vite proxy in dev)
# VITE_API_URL=http://localhost:3001
